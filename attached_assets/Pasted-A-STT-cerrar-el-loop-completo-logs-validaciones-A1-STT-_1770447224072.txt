A) STT: cerrar el loop completo (logs + validaciones)
A1) STT logs deben marcarse como confirmados al aplicar

Ahora existe /api/ai/apply, pero no actualiza el stt_logs con confirmed/result.

Hacer:

En shared/schema.ts (o donde esté stt_logs):

asegurar columnas:

confirmed boolean default false

resultEntityType (text/varchar) nullable

resultEntityId (text/varchar) nullable

confirmedAt timestamp nullable

En server/storage/stt.ts:

agregar updateSttLogConfirmed(logId, tenantId, updates):

set confirmed=true, resultEntityType, resultEntityId, confirmedAt=now

En server/routes/stt.ts → POST /api/ai/apply:

luego de crear el recurso real, llamar a updateSttLogConfirmed

validar que el log pertenezca al tenant (tenant isolation)

A2) Missing fields: no crear recursos si faltan campos mínimos

En /api/ai/apply:

si intent CREATE_PRODUCT y falta name o price

si CASH_MOVEMENT falta amount o direction (IN/OUT)

si CREATE_ORDER falta “algo mínimo” (ej. title o description)
→ devolver 400 con { error: "MISSING_FIELDS", missing_fields:[...] }

B) Branch-scope blindado + Caja por sucursal (open sessions independientes)
B1) Arreglar “open cash session” por sucursal

Hoy getOpenSession(tenantId) no discrimina branchId, entonces bloquea abrir una caja por sucursal.

Hacer:

Cambiar storage:

getOpenSession(tenantId, branchId) → filtra por ambos

openCashSession(tenantId, branchId, userId, openingAmount) → crea sesión por sucursal

closeCashSession(tenantId, branchId, ...) igual

Cambiar rutas:

POST /api/cash/sessions/open (o equivalente):

si scope=BRANCH forzar branchId del token

validar que branchId pertenezca al tenant

permitir 1 sesión abierta por sucursal (no global)

Actualizar cualquier query que liste sesiones/movimientos:

si scope=BRANCH filtrar branchId

B2) Blindaje global branch-scope

Crear helper único (si no existe):

getEffectiveBranchId(req):

si scope=BRANCH → token.branchId

si scope=TENANT → body/query.branchId opcional
Aplicarlo en:

orders list/get/create/update

cash sessions/movements

expenses (si existen)

products stock endpoints

Asegurar: un BRANCH user no puede ver ni tocar otra sucursal aunque cambie IDs.

C) Delivery addon: Google Directions con paradas en orden de clic (waypoints)
C1) Guardar origin/destination + waypoints en la ruta y construir link Directions mínimo

Si integrar Maps JS es mucho ahora, mínimo entregar el “core”:

generar un link Google Maps Directions con:

origin = dirección origen (sucursal o config)

waypoints = paradas intermedias (en orden de clic)

destination = última parada
Formato:
https://www.google.com/maps/dir/?api=1&origin=...&destination=...&waypoints=...

Hacer:

En backend al crear ruta (delivery):

guardar en delivery_routes:

originAddress

directionsUrl

opcional distanceMeters, durationSeconds si luego usás API

En UI del delivery panel:

mostrar botón “Abrir ruta” → abre directionsUrl

mostrar lista stops en orden con numeración clara

C2) (Opcional si se puede) Directions API

Si ya está previsto key:

endpoint backend GET /api/delivery/routes/:id/directions que llama Directions API y guarda polyline.
Pero si no da tiempo, con URL bien armada ya cumple el requisito de “punto A → paradas”.

D) Seeds OFF por defecto

Ahora seedea por default. Cambiar a:

seed solo si SEED === "true"

En server/index.ts:

const shouldSeed = process.env.SEED === "true";
Actualizar README.

E) Sub-tenants (Sucursales): credenciales y acceso al panel sucursal

El problema actual: no hay forma simple en UI/API para que un tenant cree usuarios de sucursal con credenciales y puedan loguear.

E1) Backend: CRUD de usuarios de sucursal (tenant-only)

Crear endpoints protegidos (tenant scope TENANT solamente):

POST /api/branch-users

body: { branchId, dni, firstName, lastName, phone, pinOrPassword }

crea user con scope="BRANCH" y branchId

guarda credencial hash (bcrypt)

role por defecto BRANCH_STAFF

GET /api/branch-users?branchId=...

PATCH /api/branch-users/:id (activar/desactivar, reset pin)

POST /api/branch-users/:id/reset-credentials → genera nuevo PIN temporal y lo devuelve al tenant admin

Reglas:

solo tenant admin/staff TENANT puede administrar branch users

un branch user nunca puede ver usuarios

E2) Login de sucursal (sin endpoint nuevo, usando el mismo login)

En /api/auth/login:

si user.scope = BRANCH:

devolver token con {scope:"BRANCH", branchId}

frontend redirige a /app/branch (panel sucursal)

E3) Frontend: pantalla para gestionar credenciales de sucursales

Agregar en panel tenant:

Menú “Sucursales” → dentro de cada sucursal un tab “Personal / Accesos”

Permitir:

crear usuario sucursal

ver lista (dni, nombre, teléfono, activo)

resetear PIN (mostrar modal con el PIN nuevo)

desactivar usuario

En login UI:

misma pantalla: tenant_code + dni + pin/password

si el token trae scope=BRANCH → ir al panel sucursal automáticamente.

E4) Panel sucursal

Asegurar que existe ruta /app/branch y muestra solo:

pedidos de su sucursal

caja de su sucursal (si plan lo permite)

productos/stock de su sucursal (si aplica)

F) Validaciones finales (obligatorio)

Después de implementar, dejar una sección “Cómo probar”:

Crear tenant + plan

Crear 2 sucursales

Crear 2 usuarios sucursal (uno por sucursal) con PIN

Loguear como sucursal A → no ve nada de sucursal B

Abrir caja en sucursal A y sucursal B en paralelo

STT: transcribe + apply crea un pedido real y marca stt_log confirmado

Delivery: crear pedidos con delivery, seleccionar en orden, generar directionsUrl con paradas

Confirmar SEED no corre salvo SEED=true

Empezá por E (credenciales sucursales) y B (caja por sucursal), luego A, luego D, luego C.